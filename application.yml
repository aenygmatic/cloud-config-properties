server:
    port: 80

#---
#spring:
#  profiles: cloudfoundry
eureka:
  client:
    serviceUrl:
      defaultZone: http://v2-andromeda-eureka.grc-apps.svc.ice.ge.com:80/eureka/
  instance:
    hostname: '${CF_INSTANCE_ADDR}'
    nonSecurePort: 80
